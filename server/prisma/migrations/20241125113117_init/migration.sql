/*
  Warnings:

  - You are about to drop the column `user_position` on the `organisation_users` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `organisation_users` DROP COLUMN `user_position`;
